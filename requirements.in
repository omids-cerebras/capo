# Run `./pin.sh` to generate a fully pinned requirements file
# (pinned-requirements.txt) for reproducible installs.

# --- Runtime dependencies (keep in sync with pyproject.toml) ---------
torch>=2.2
numpy>=1.24
verl>=0.3.0.post1

# --- Dev / testing dependencies --------------------------------------
pytest>=7.0
black