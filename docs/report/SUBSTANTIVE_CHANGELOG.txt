SUBSTANTIVE CHANGE LOG (section-by-section)

0. Abstract
- Added an explicit statement that the paper provides robustness guarantees for plug-in inverse-variance weighting, aligning the abstract with the strengthened theory in Section 3.

1. Introduction (Section 1)
- Added a new “Related work and connections” paragraph that situates CAPO within: (i) RLHF/RLVR optimization pipelines (PPO-type updates, preference optimization), (ii) classical WLS/GLS estimation theory, and (iii) empirical Bayes methodology.
  * Why it matters: clarifies novelty relative to existing length-normalization heuristics and places CAPO on a standard statistical footing.
- Added an “Organization” paragraph with explicit forward references to sections/appendices.
  * Why it matters: improves journal-style navigability and makes the logical dependency structure explicit.

2. Problem Setup (Section 2)
2.1 Setup and Notation (new)
- Introduced a dedicated “Setup and Notation” subsection with:
  * A unified definition of token-level contributions Z_{i,t}, trajectory gradients g_i as token sums, the proxy variance v_i via a scalar functional Ψ, and the unbiasedness scaling constant M.
  * A notation table (Table 1) consolidating symbols used across all sections.
  * Two explicit standing assumptions:
    - Cross-trajectory uncorrelatedness (Assumption 2.1), used to justify additive variance decompositions and inverse-variance optimality.
    - Second-order stationarity within trajectories (Assumption 2.2), used to define and interpret lag-covariance Γ_i(k).
  * Why it matters: removes implicit assumptions and standardizes notation for the subsequent theorem statements and proofs.
- Added a missing equation label for the lag-covariance definition Γ_i(k) (Eq. (2.2) label eq:Gamma_def).
  * Why it matters: enables precise cross-referencing of the dependence model used throughout the theory.

2.2–2.4 Existing setup subsections
- No scope reduction; preserved all original definitions and variance decompositions, but connected them explicitly to the new assumptions and notation table.

3. Covariance-Aware Policy Optimization (Section 3)
3.1 Minimum-variance unbiased aggregation
- Replaced the one-line MVU proof with a full, step-by-step Lagrange-multiplier derivation (Lemma 3.1), including uniqueness via strict convexity.
  * Why it matters: upgrades a “sketch” into a referee-grade proof and makes the optimization problem explicit.
- Added an explicit displayed equation with label for the ΔL inverse-length special case (Eq. (3.3) label eq:deltaL_alpha1).
  * Why it matters: resolves an undefined reference and makes the ΔL connection formally citable in later arguments.
- Added a new robustness result for estimated variances (Proposition 3.2):
  * If variance estimates are within a multiplicative factor c, the resulting plug-in inverse-variance estimator’s variance is within a factor c^2 of the oracle MVU optimum.
  * Why it matters: provides a formal stability guarantee under model misspecification and finite-sample EB estimation error.

3.2 Scalarization vs. vector-valued objectives (expanded)
- Added a new common-shape covariance assumption (Assumption 3.1) and a corresponding optimality theorem (Proposition 3.3) showing that inverse-variance weights remain optimal for a broad class of matrix-valued risk functionals Ψ when per-trajectory covariances differ only by a scalar factor.
  * Why it matters: justifies the paper’s scalar proxy approach as genuinely optimal in a common high-dimensional regime, rather than merely a heuristic projection argument.

3.3 Bayesian/EB weighting foundations (corrected and expanded)
- Corrected and strengthened the “Posterior-precision aggregator” theorem:
  * Replaced the incorrect “average the conditional MVU weights over the posterior” argument with a rigorously stated scale-cancellation result under factorized variances v_i(θ)=σ^2 f_i(θ) (Theorem 3.4).
  * Added an explicit remark distinguishing (i) exact Bayesian averaging over θ from (ii) the posterior-precision approximation used under EB/concentration.
  * Why it matters: fixes a formal error, clarifies when the ratio-of-expectations form is exact, and makes the EB approximation explicit and defensible.

3.4 Dependence models and non-i.i.d. covariance (expanded)
- Activated and integrated the previously commented-out “compound symmetry / non-vanishing constant-lag covariance” subsection (Section 3.7), including:
  * A new variance expression under compound symmetry (Lemma 3.6).
  * A corresponding optimal weighting discussion and its implications for long-context regimes where long-range dependence does not decay.
  * Why it matters: materially expands the dependence modeling scope and addresses a realistic failure mode of finite-bandwidth assumptions in LLM CoT.

3.5 Empirical Bayes theory (minor strengthening)
- Replaced an in-text book reference with a proper citation to Asymptotic Statistics (van der Vaart) and ensured the bibliography contains the corresponding entry.
  * Why it matters: improves auditability of the asymptotic arguments used to justify EB consistency/normality.

4. Algorithms (Section 4)
- Added a new paragraph on numerical stability and identifiability, including log-space weight computation, log-sum-exp normalization, and practical EB regularization guidance.
  * Why it matters: makes the algorithms implementable in long-context regimes where naïve weight computation underflows/overflows, and clarifies when β and ξ are weakly identifiable.
- Added an explicit section label (sec:algorithms) to support cross-references from the Introduction and Abstract.
  * Why it matters: compilation correctness and structural clarity.

5. Evaluations (Section 5)
- Expanded the experimental protocol description:
  * Added an explicit paragraph describing the group-based objective and what is held fixed across methods (only normalization/aggregation varies).
  * Added a reproducibility paragraph and a new hyperparameter table placeholder (Table 5.1) to make missing experimental metadata explicit rather than implicit.
  * Why it matters: aligns reporting with journal expectations for controlled comparisons.
- Added citations to baseline definitions (ΔL, GRPO/DeepSeekMath, DAPO, Dr. GRPO) directly in the baseline list.
  * Why it matters: makes the baseline set auditable and properly attributed.
- Added a new “Additional diagnostics and failure modes” subsection:
  * Weight degeneracy diagnostics (ESS/entropy), length-conditioned bias checks, and a model-misspecification discussion linked back to the dependence theory.
  * Why it matters: operationalizes theoretical variance arguments into measurable stability checks and clarifies how to interpret failures.

6. Contributions (Section “Summary of contributions”)
- Expanded the contributions list to include the new vector-valued optimality result (common-shape covariance) and the new robustness theorem for plug-in weights.
  * Why it matters: keeps the stated contributions aligned with the strengthened technical content.

7. Conclusion and Acknowledgment
- Added a full conclusion section summarizing the theoretical and practical contributions and outlining limitations/future work (cross-trajectory dependence; heavy tails).
  * Why it matters: journal-style closure and explicit delineation of open problems.
- Added a minimal acknowledgment section (kept generic to avoid introducing unverified factual claims).

8. Bibliography
- Added foundational and contextual references required by the expanded introduction and theory:
  * PPO (Schulman et al.), InstructGPT (Ouyang et al.), DPO (Rafailov et al.), GLS/time-series reference (Hamilton), empirical Bayes reference (Efron), and Asymptotic Statistics (van der Vaart).
  * Why it matters: supports the expanded literature positioning and formal asymptotic citations.

Package additions
- None. The document class/theme and the existing package ecosystem were preserved exactly as in the uploaded project.
